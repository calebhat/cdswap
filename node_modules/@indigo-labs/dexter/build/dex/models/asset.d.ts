export declare class Asset {
    policyId: string;
    nameHex: string;
    decimals: number;
    constructor(policyId: string, nameHex: string, decimals?: number);
    static fromId(id: string, decimals?: number): Asset;
    id(dilimeter?: '' | '.'): string;
    get assetName(): string;
}
export type Token = Asset | 'lovelace';
